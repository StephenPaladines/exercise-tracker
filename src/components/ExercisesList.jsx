import React, { useState } from "react";

const ExercisesList = () => {
  const [{ exercise }, setExerciseList] = useState({
    description: "",
    duration: ""
  });
  let exerciseList = [];
  return <div></div>;
};

export default ExercisesList;
